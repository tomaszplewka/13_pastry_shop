import styled from 'styled-components';

export const AuthContainer = styled.div`
    position: relative;
    padding: 5rem 2rem;
    background-color: var(--color-blue);
    background: radial-gradient(circle, rgba(245,245,245,1) 0%, rgba(58,110,165,1) 100%);
`;